<Window x:Class="NEUNetworkAutoLogin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="东北大学校园网自动登录"
        Height="780"
        Width="980"
        MinHeight="680"
        MinWidth="920"
        FontFamily="Microsoft YaHei UI"
        Background="#F2F6FB"
        WindowStartupLocation="CenterScreen"
        StateChanged="Window_StateChanged"
        Closing="Window_Closing">
  <Window.Resources>
    <LinearGradientBrush x:Key="HeaderBrush" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#0F5FD6" Offset="0" />
      <GradientStop Color="#2A8DFF" Offset="1" />
    </LinearGradientBrush>

    <Style x:Key="CardStyle" TargetType="Border">
      <Setter Property="CornerRadius" Value="16" />
      <Setter Property="Background" Value="White" />
      <Setter Property="Padding" Value="14" />
      <Setter Property="Margin" Value="0,0,0,10" />
    </Style>

    <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
      <Setter Property="Height" Value="34" />
      <Setter Property="Padding" Value="10,6" />
      <Setter Property="BorderBrush" Value="#CBD8EA" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Background" Value="#FCFDFF" />
    </Style>

    <Style TargetType="PasswordBox">
      <Setter Property="Height" Value="34" />
      <Setter Property="Padding" Value="10,6" />
      <Setter Property="BorderBrush" Value="#CBD8EA" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Background" Value="#FCFDFF" />
    </Style>

    <Style TargetType="Button">
      <Setter Property="Height" Value="36" />
      <Setter Property="Margin" Value="0,0,8,0" />
      <Setter Property="Padding" Value="10,6" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Background" Value="#1678F2" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
  </Window.Resources>

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" MinHeight="250" />
      <RowDefinition Height="8" />
      <RowDefinition Height="2*" MinHeight="320" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" CornerRadius="20" Padding="18" Margin="0,0,0,10" Background="{StaticResource HeaderBrush}">
      <DockPanel LastChildFill="True">
        <Border Width="56" Height="56" CornerRadius="28" Background="#EAF3FF" Margin="0,0,14,0" DockPanel.Dock="Left">
          <TextBlock Text="NEU" Foreground="#0D56C8" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <StackPanel>
          <TextBlock Text="东北大学校园网自动登录" Foreground="White" FontSize="28" FontWeight="Bold" />
          <TextBlock Text="自动检测网络状态并执行统一认证登录" Foreground="#E7F2FF" Margin="0,4,0,0" />
        </StackPanel>
      </DockPanel>
    </Border>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <StackPanel>
        <Border Style="{StaticResource CardStyle}">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="配置中心" FontSize="18" FontWeight="Bold" Foreground="#1F2D3D" Margin="0,0,0,10" />

            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,8">
              <TextBlock Text="统一认证账号" Margin="0,0,0,4" Foreground="#425466" />
              <TextBox x:Name="UsernameBox" Style="{StaticResource InputTextBoxStyle}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,10,8">
              <TextBlock Text="统一认证密码" Margin="0,0,0,4" Foreground="#425466" />
              <PasswordBox x:Name="PasswordBox" />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,22,0,8">
              <CheckBox x:Name="StartupCheckBox" Content="开机自动启动并后台运行" FontWeight="SemiBold" Foreground="#1F2D3D" />
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,8">
              <TextBlock Text="Portal Host（门户地址）" Margin="0,0,0,4" Foreground="#425466" />
              <TextBox x:Name="PortalHostBox" Style="{StaticResource InputTextBoxStyle}" />
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,10,8">
              <TextBlock Text="Service Base URL（认证回跳地址）" Margin="0,0,0,4" Foreground="#425466" />
              <TextBox x:Name="ServiceBaseUrlBox" Style="{StaticResource InputTextBoxStyle}" />
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,8">
              <TextBlock Text="AC ID" Margin="0,0,0,4" Foreground="#425466" />
              <TextBox x:Name="AcIdBox" Style="{StaticResource InputTextBoxStyle}" />
            </StackPanel>
          </Grid>
        </Border>

        <Border Style="{StaticResource CardStyle}" Margin="0">
          <DockPanel LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10">
              <Button x:Name="SaveButton" Content="保存配置" Click="SaveButton_Click" />
              <Button x:Name="LoginButton" Content="登录" Click="LoginButton_Click" />
              <Button x:Name="LogoutButton" Content="注销" Click="LogoutButton_Click" />
              <Button x:Name="StartButton" Content="启动监控" Click="StartButton_Click" />
              <Button x:Name="StopButton" Content="停止监控" Click="StopButton_Click" />
              <Button x:Name="OpenLogsButton" Content="打开日志目录" Click="OpenLogsButton_Click" />
              <Button x:Name="MinimizeButton" Content="最小化到托盘" Margin="0" Click="MinimizeButton_Click" />
            </StackPanel>
            <Border CornerRadius="10" Background="#F4F8FF" Padding="10" BorderBrush="#D4E3FA" BorderThickness="1">
              <TextBlock x:Name="StatusTextBlock" Text="就绪" FontWeight="SemiBold" Foreground="#244A7C" />
            </Border>
          </DockPanel>
        </Border>
      </StackPanel>
    </ScrollViewer>

    <GridSplitter Grid.Row="2"
                  Height="8"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Center"
                  Background="#D7E2F2"
                  ShowsPreview="True"
                  ResizeDirection="Rows"
                  ResizeBehavior="PreviousAndNext" />

    <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0" Padding="12">
      <DockPanel LastChildFill="True">
        <TextBlock Text="运行日志" FontSize="16" FontWeight="Bold" Foreground="#1F2D3D" DockPanel.Dock="Top" Margin="0,0,0,8" />
        <TextBox x:Name="LogTextBox"
                 FontFamily="Consolas"
                 FontSize="13.5"
                 MinHeight="260"
                 VerticalContentAlignment="Top"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 Background="#FFFFFF"
                 Foreground="#111111"
                 BorderBrush="#CCD5E4"
                 BorderThickness="1"
                 Padding="12,10" />
      </DockPanel>
    </Border>
  </Grid>
</Window>
